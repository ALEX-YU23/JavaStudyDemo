package org.jgs.oop.day02;
/*
 * 测试学生类
 * new Student()  类名()  Student() 构造方法  特殊方法 用于创建对象
 * 1.如果类中没有显示定义构造方法 虚拟机会帮我们自动提供一个无参的构造方法
 * 2.如果类中有定义构造方法 虚拟机不在提供一个无参的构造方法
 * 普通方法：
 *  权限修饰符 返回值 方法名(参数列表){}
 * 
 * 构造方法：
 *  权限修饰符(public) 方法名(参数列表){方法体}
 *  1.没有返回值  不要写void
 *  2.构造方法名必须和类名保持一致
 *  3.构造方法用于创建对象   使用关键字new 来调用
 *  4.构造方法也是可以重载的
 *  5.构造方法之间可以互相调用 使用this() 但是要保证this() 要在第一行
 *  6.构造方法可以调用普通的方法 反之不行
 *  
 * 
 */
public class TestStudent {

	public static void main(String[] args) {
		
		//创建学生对象
		 Student student = new Student("张三",20,1.8, '男');
//		 Student student = new Student("张三",20);
		//赋值
//		 student.name= "张三";
//		 student.age=20;
//		 student.sex='男';
//		 student.height = 1.8;
		 
		 //调用方法赋值 
		 //student.setValue("张三",20,1.8, '男');
		//调用方法  对象名.方法名
//		 student.study();
		 System.out.println(student.name);
		 System.out.println(student.age);
		 System.out.println(student.height);
		
	}

}
